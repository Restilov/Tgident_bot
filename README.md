# Telegram Identity Bot

A simple bot for developers to retrieve Telegram user and chat identity details. You can just read the code, it's simple.

Clone the repo and run:
```bash
docker compose up --build -d
```
